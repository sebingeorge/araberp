@using ArabErp.Domain
@model IEnumerable<PendingPurchaseRequest>
@{
    ViewBag.Title = "PendingSupplyOrder";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="container">
    <div class="row">
        <div class="form-head1">
            <h4>Pending GRN</h4>
        </div>
        <div class="main-part pad1">
            <form method="post" action="@Url.Action("Create", "SupplyOrder")">
                @*<div class=" table-responsive">
                    <table class="table table-bordered">
                        <thead>
                            <tr class="grid_heading">
                                <th><h1>PRS No.</h1></th>
                                <th><h1>PRS Date</h1></th>
                                <th><h1>Material Description</h1></th>
                                <th class="text-right "><h1>PR Qty.</h1></th>
                                <th class="text-right "><h1>Pending Qty.</h1></th>
                                <th class="text-right "><h1>Unit</h1></th>
                                <th><h1>Required Date</h1></th>
                                <th><h1>Select</h1></th>
                            </tr>
                        </thead>
                        <tbody class="grid_row">

                    </table>
                </div>*@
                <div class="main-part pad1">
                    <div class="table-responsive">


                        <div class="row border">

                            <div class="col-md-12">

                                <div class="col-md-3 grid-left">PRS No. and Date</div>
                                <div class="col-md-3 grid-left">Required Date</div>
                                <div class="col-md-3 grid-left">Select</div>

                            </div>
                            @foreach (var item in Model)
                            {
                                <div class="col-md-2 grid_row"> @item.PurchaseRequestNo  @item.RequiredDate.ToString("dd/MMM/yyyy")  </div>
                                <div class="col-md-2 grid_row">@item.RequiredDate.ToString("dd/MMM/yyyy") </div>
                                <div class="col-md-2 mtop form-button2 form-button1">@Html.ActionLink("Select", "Create", "SupplyOrder", new { WorkShopRequestId = @item.PurchaseRequestId }, null) </div>
                            }
                        </div>

                    </div>
                </div>
                <div class="row">


                    <div class="col-md-12">
                        <div class="margl">
                            <button class="form-button">Previous</button>
                        </div>
                        <div class="margf">
                            <button type="submit" class="form-button">Exit</button>
                       
                   
                            <button type="submit" class="form-button">Proceed</button>
                        </div>

                    </div>


                </div>
            </form>
        </div>
    </div>
</div>
