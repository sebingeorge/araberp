@model IList<ArabErp.Domain.Item>
@using GridMvc.Html

<script>
    @*function OpenEdit(id) {
        var url = '@Url.Action("Edit")';
        //url += "?Id=" + id;
        Location.href = url;
        return false;
     
    }
    function OpenDelete(id) {
        var url = '@Url.Action("Delete")';
        //url += "?Id=" + id;
        Location.href = url;
        return false;
    }*@
</script>

<style type="text/css">
    .tableData {
        font-size: 12px;
        padding-left: 4px;
    }

    .list {
        display: block;
        height: 320px;
        overflow-y: scroll;
    }

        .list tr:hover {
            background-color: #DFEDF7;
        }
      
</style>

<div class="table-responsive">
    <div class="main-grid main-grid-top">

        @Html.Grid(Model).Columns(columns =>
                        {
                            columns.Add(m => m.PartNo).Titled("Part No.").SetWidth(30);
                            columns.Add(m => m.ItemName).Titled("Item Name").SetWidth(110);
                            columns.Add(m => m.CategoryName).Titled("Item Category").SetWidth(110);
                            columns.Add(m => m.ItemGroupName).Titled("Item Group").SetWidth(110);
                            columns.Add(m => m.ItemSubGroupName).Titled("Item Sub Group").SetWidth(110);
                            columns.Add(m => m.UnitName).Titled("Unit").SetWidth(40);
                            @*columns.Add().Encoded(false).Sanitized(false).SetWidth(30).Css("cent").RenderValueAs(o => @<button class="edit-book display-mode" onclick="Edit(this.)">Edit</button>);
                            columns.Add().Encoded(false).Sanitized(false).SetWidth(30).Css("cent").RenderValueAs(o => @<a href="">Delete</a>);*@
                            columns.Add().Encoded(false).Sanitized(false).SetWidth(30).Css("cent").RenderValueAs(o => @<a href="@Url.Action("Edit", new { Id = o.ItemId})">Edit</a>);
                            columns.Add().Encoded(false).Sanitized(false).SetWidth(30).Css("cent").RenderValueAs(o => @<a href="@Url.Action("Delete", new { Id = o.ItemId})">Delete</a>);
                            //columns.Add().Encoded(false).Sanitized(false).SetWidth(30).RenderValueAs(o => Html.ActionLink("Edit",1));
                            //columns.Add(m => m.ItemId).Titled("Vip customer").SetWidth(150).RenderValueAs(o => Html.ActionLink);
                            //columns.Add(m => m.ItemId).RenderValueAs(m => m.ItemId ? Html.ActionLink("Edit", "Edit") : m.ItemId.ToString());
                            //columns.Add(m => m.ItemId);

                            //.Format("<a href="\"DeleteRecord?name={CategoryName}\"">{CategoryName}</a>");


                            //columns.Add(m => m.CurrencyName).Sortable(true);
                        }).WithPaging(10)
        </div>

    <div class="row-count1">
        <h5>Row Count:- @Model.Count()</h5>
    </div>


    </div>


