@model  ArabErp.Domain.WorkShopRequest
@{
    ViewBag.Title = "Create WorkShop Request";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<script>
    $(document).ready(function() {
        $('#work_form').bootstrapValidator({
            fields: {
                WorkShopRequestNo: {
                    validators: {
                        notEmpty: {
                            message: 'Please enter request no'
                        }
                    }
                },
                RequiredDate: {
                    validators: {
                        notEmpty: {
                            message: 'Please enter required date'
                        }
                    }
                }

         }
        });

    });
   
    $(document).ready(function () {
        $("#Items_0__SlNo").val('1');
            $("#add_row").click(function () {
                var i = 0;
                var no = 1;
                $('input[id^="Row_"]').each(function () {
                    var id = this.id;
                    no++;
                    var row = Number(id.replace("Row_", ""));
                    if(row>i)
                    {
                        i = row;
                    }
                });
                i++;

                var index = (new Date()).getTime(); // unique indexer
                var clone = $('#Row_0').clone(); // clone the BatchProducts item
                //Update the index of the clone
                       
                clone.html($(clone).html().replace(/\Items_0__/g, 'Items_' + i + '__'));
                clone.html($(clone).html().replace(/\Items[0]./g, 'Items[' + i + '].'));
                clone.html($(clone).html().replace(/\[0\]/g, '[' + i + ']'));
                clone.html($(clone).html().replace(/\(0\)/g, '(' + i + ')'));
                clone.html($(clone).html().replace(/Row_0/, 'Row_' + i + ''));
                clone.html($(clone).html().replace(/slno1/, 'slno' + (i + 1) + ''));
                $("#tab_logic").append('<tr id="' + i + '">' + clone.html() + '  </tr>');
            });
        });

      
  
        $(document).ready(function () {
            $('#WorkShopRequestDate')
                .datepicker({
                    format: 'dd/MM/yyyy'
                });

            $('#RequiredDate')
                .datepicker({
                    format: 'dd/MM/yyyy'
                })

        });

   
    //function calculateSummary() {
    //    var quantity = parseFloat("0");
    //    var amount = parseFloat("0");
    //    $('.clQuantity').each(function (i, obj) {
    //        var a = parseFloat($("#Items_" + i + "__Quantity").val());
    //        console.log(a);
    //        if (isNaN(a)) {
    //            a = parseFloat("0");
    //        }

    //        quantity = quantity + a;
    //    });
          
    //    $("#QtyTotal").text(quantity);
     
    //}
</script>
                   
<div class="container">
    <div class="row">
        <div class="form-head1">
            <h4>WorkShop Request</h4>
        </div>
     
        <div class="main-part pad1">
            @using (Html.BeginForm("Save", null, FormMethod.Post, new { @class = "createFrom", @id = "work_form" }))
            {
                @Html.HiddenFor(m => m.SaleOrderId, new { @class = "form_input", autocomplete = "off" })
                @Html.HiddenFor(m => m.CustomerId, new { @class = "form_input", autocomplete = "off" })
                <div class="row form-group">
                    <div class="col-md-2">
                        <label for="reqno">Request No.</label>
                        @Html.TextBoxFor(m => m.WorkShopRequestNo, new { @class = "form_input", autocomplete = "off" })
                    </div>
                    <div class="col-md-2 col-md-offset-8">
                        <label for="inputDate">Date</label>
                        @Html.TextBoxFor(m => m.WorkShopRequestDate, String.Format("{0:dd/MM/yyyy}", Model.WorkShopRequestDate), new { @class = "form_input ", autocomplete = "off", @readonly = "readonly" })
                    </div>
                </div>
    @Html.Action("SaleorderData")
                @*<div class="row form-group">
                    <div class="col-md-3">
                        <label for="sono">Sale Order No.</label>
                        @Html.TextBoxFor(m => m.SaleOrderRefNo, new { @class = "form_input", autocomplete = "off",@readonly ="readonly" })
                    </div>
                    <div class="col-md-3">
                        <label for="cus">Customer</label>
                        @Html.TextBoxFor(m => m.CustomerName, new { @class = "form_input", autocomplete = "off", @readonly = "readonly" })
                    </div>
                    <div class="col-md-3">
                        <label for="cusref">Customer Order Ref.</label>
                        @Html.TextBoxFor(m => m.CustomerOrderRef, new { @class = "form_input", autocomplete = "off", @readonly = "readonly" })
                    </div>
                </div>
                <div class="row form-group">
                    <div class="col-md-3">
                        <label for="productr">Product</label>
                        <select id="productr" autocomplete="off" class="form_input">
                            <option>Select Product</option>
                            <option>Product1</option>
                            <option>Product2</option>
                            <option>Product3</option>
                        </select>
                    </div>
                    <div class="col-md-3">
                        <label for="qty">Qty.</label>
                        <input type="text" id="qty" autocomplete="off" class="form_input" />
                    </div>
                </div>*@
   
    <div class="table-responsive">
        <table class="table table-bordered table-hover" id="tab_logic">
            <thead>
                <tr class="grid_heading">
                    <th width="5%">#</th>
                    <th width="10%">Part No</th>
                    <th width="20%">Material/Spares</th>
                    <th width="25%">Remarks</th>
                    <th width="5%" class="text-right ">Qty</th>
                    <th width="10%">Unit</th>
                </tr>
            </thead>
            <tbody>
                @for (int i = 0; i < Model.Items.Count; i++)
                {
                    <tr id="Row_@i">
                        <td>
                            <span id="slno@(i+1)">@(i + 1)</span>
                          
                        </td>
                        <td>@Html.TextBoxFor(m => m.Items[i].PartNo, new { @class = "form_input", autocomplete = "off", placeholder = "PartNo" })</td>
                        <td>
                            <input type="hidden" id="Row_@i" />
                            @Html.DropDownListFor(m => m.Items[i].ItemId, (SelectList)ViewBag.ItemList, "Select Material/Spares", new { @class = "form_input", autocomplete = "off" })
                        </td>
                        <td>@Html.TextBoxFor(m => m.Items[i].ItemDescription, new { @class = "form_input", autocomplete = "off", placeholder = "Remarks" })</td>
                      

                        <td>@Html.TextBoxFor(m => m.Items[i].Quantity, new { @class = "form_input clQuantity", @style = "text-align:right", autocomplete = "off", placeholder = "Qty" })</td>
                        <td>
                            @Html.DropDownListFor(m => m.Items[i].UnitId, (SelectList)ViewBag.unitlist, "Select Unit", new { @class = "form_input", autocomplete = "off" })
                        </td>
                    </tr>
                }
                @*<tr id='addr1'></tr>*@
            </tbody>
            <tfoot>
                <tr class="grid_heading">
                    <th width="5%"></th>
                    <th width="20%"></th>
                    <th width="25%"></th>
                    <th width="10%"></th>
                    <th width="5%" class="text-right "></th>
                    <th width="10%"></th>
              
                </tr>
            </tfoot>
        </table>
    </div>
    <a id="add_row" class="glyphicon glyphicon-plus"></a><a id='delete_row' style="float:right" class="glyphicon glyphicon-minus"></a>


                <div class="row form-group">
                    <div class="col-md-12">
                        <label for="SplRemarks1">Spl.Remarks</label>
                        @Html.TextAreaFor(m => m.SpecialRemarks, new { @class = "form_input", autocomplete = "off" })
                    </div>
                </div>
                <div class="row form-group">
                    <div class="col-md-2">
                        <label for="reqdate">Delivery Date</label>
                        @Html.TextBoxFor(m => m.RequiredDate, new { @class = "form_input", autocomplete = "off" })
                    </div>
                </div>
    <div class="marg">
        <button class="form-button">Exit</button>
        <button type="submit" class="form-button">Save</button>

    </div>

            }
        </div>
        </div>
</div>


