@model IList<ArabErp.Domain.Bay>
@using GridMvc.Html

<script>
    function OpenEdit(id) {
       alert("open");
        @*var url = '@Url.Action("Edit")';
        url += "?Id=" + id;
        location.href = '@Url.Action("Edit", "Item")';*@

        var url = '@Url.Action("Edit")';
        //url += "?Id=" + id;
        Location.href = url;
        return false;

    }

    function OpenDelete(id) {
        //alert("open");
        @*var url = '@Url.Action("Edit")';
        url += "?Id=" + id;
        location.href = '@Url.Action("Edit", "Item")';*@

        var url = '@Url.Action("Delete")';
        //url += "?Id=" + id;
        Location.href = url;
        return false;
    }

</script>

<style type="text/css">
    .tableData {
        font-size: 12px;
        padding-left: 4px;
    }

    .list {
        display: block;
        height: 320px;
        overflow-y: scroll;
    }

        .list tr:hover {
            background-color: #DFEDF7;
        }
</style>

<div class="table-responsive">

    <div class="main-grid">

        @Html.Grid(Model).Columns(columns =>
                        {
                            //columns.Add(m => m.UnitRefNo).Titled("ID").SetWidth(110);
                            columns.Add(m => m.BayRefNo).Titled("Ref No.").SetWidth(20);
                            columns.Add(m => m.BayName).Titled("Name").SetWidth(200);
                            columns.Add().Encoded(false).Sanitized(false).SetWidth(30).Css("cent").RenderValueAs(m => @<a href="@Url.Action("Edit", new { Id = m.BayId })">Edit</a>);
                            columns.Add().Encoded(false).Sanitized(false).SetWidth(30).Css("cent").RenderValueAs(m => @<a href="@Url.Action("Delete", new { Id = m.BayId })">Delete</a>);


                            //.Format("<a href="\"DeleteRecord?name={CategoryName}\"">{CategoryName}</a>");

                            //columns.Add(m => m.CurrencyName).Sortable(true);
                        }).WithPaging(10)

    </div>


    <div class="row-count1">
        <h5>Row Count:- @Model.Count()</h5>
    </div>


</div>




