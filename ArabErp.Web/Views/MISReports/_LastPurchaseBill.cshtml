@model IList<ArabErp.Domain.PurchaseBill>

<strong>Last Purchase Bills</strong>
<div class="div-bordered">
    <table class="table-responsive table-hover table-bordered full-width-table">
        <thead class="grid_heading">
            <tr>
                <th>Purchase Bill No &amp; Date</th>
                <th>Request Quantity</th>
            </tr>
        </thead>
        <tbody class="grid_row">
            @if (Model != null && Model.Count > 0)
            {
                foreach (var item in Model)
                {
                    <tr>
                        <td><strong>@item.PurchaseBillRefNo</strong> - @item.PurchaseBillDate.ToString("dd/MMMM/yyyy")</td>
                        <td>@item.PurchaseBillAmount</td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td align="center" colspan="2">
                        <em>No items found!</em>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

